// Primitive data types are `String`, `Number`, `Boolean`, `undefined` , `null`

let name = 'Brian'; // string

console.log(name);

let age = 30; // number

console.log(age);

let is_approved = true; // boolean

console.log(is_approved);

let firstname; //undefined

console.log(firstname);

let selected_color = null; // clear the value of the variable

console.log(selected_color);